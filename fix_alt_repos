---
# Возврат репозиториев ALT Linux с https обратно на http
# Запускать для группы all_clients

- name: Revert ALT Linux repositories (https -> http)
  hosts: all_clients
  become: yes
  gather_facts: no
  ignore_unreachable: yes

  tasks:
    - name: Find all .list files in /etc/apt/sources.list.d
      find:
        paths: /etc/apt/sources.list.d
        patterns: '*.list'
      register: list_files
      failed_when: false

    - name: Replace https://ftp.altlinux.org with http:// in .list files
      replace:
        path: "{{ item.path }}"
        regexp: 'https://ftp\.altlinux\.org'
        replace: 'http://ftp.altlinux.org'
      loop: "{{ list_files.files | default([]) }}"
      register: replace_results

    - name: Replace in /etc/apt/sources.list (if exists)
      replace:
        path: /etc/apt/sources.list
        regexp: 'https://ftp\.altlinux\.org'
        replace: 'http://ftp.altlinux.org'
      failed_when: false
      register: replace_main

    - name: Show changed files
      debug:
        msg: >-
          Changed: {{
            (replace_results.results | default([]) | selectattr('changed') | map(attribute='item.path') | list)
            + (['/etc/apt/sources.list'] if (replace_main.changed | default(false)) else [])
          }}

    - name: Update apt cache
      apt:
        update_cache: yes
      register: apt_update
      retries: 5
      delay: 10
      until: apt_update is success

    - name: Report result
      debug:
        msg: "APT cache updated successfully on {{ inventory_hostname }}"
      when: apt_update is success
